/* 
    yarn :
        安装 ：
            1.通过npm全局安装 ：
                npm install yarn -g
                卸载 ： npm r yarn -g
            
            2.corepack :
                corepack enable
        切换版本 ：
            corepack prepare yarn@stable --activate 最新
            corepack prepare yarn@1 --activate 1.xx.xx版本
            yarn init （初始化，创建package.json）

        yarn add xxx（添加依赖）

        yarn add xxx -D（添加开发依赖）

        yarn remove xxx（移除包）

        yarn（自动安装依赖）

        yarn run（执行自定义脚本）

        yarn <指令>（执行自定义脚本）

        yarn global add（全局安装）

        yarn global remove（全局移除）

        yarn global bin（全局安装目录）
*/